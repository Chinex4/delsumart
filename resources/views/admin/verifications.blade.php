@extends('layouts.app') @section('content')<div class="mx-auto max-w-7xl px-4 py-12"><h1 class="text-3xl font-black text-blue-950">KYC verification queue</h1><div class="mt-8 space-y-4">@foreach($verifications as $v)<article class="border bg-white p-5"><div class="flex flex-wrap items-start justify-between gap-4"><div><h2 class="font-bold">{{ $v->full_name }}</h2><p class="text-sm text-slate-500">{{ $v->matric_no }} · {{ $v->programme }} · {{ $v->level }}</p><p class="mt-2 text-xs font-bold uppercase">{{ $v->verification_status }} · Resubmissions {{ $v->resubmission_count }}</p></div>@if($v->verification_status==='pending')<div class="flex gap-2"><form method="POST" action="{{ route('admin.verifications.decide',$v) }}">@csrf @method('PATCH')<input type="hidden" name="decision" value="verified"><button class="rounded-lg bg-emerald-700 px-4 py-2 text-sm font-bold text-white">Approve</button></form><form method="POST" action="{{ route('admin.verifications.decide',$v) }}" class="flex gap-2">@csrf @method('PATCH')<input type="hidden" name="decision" value="rejected"><input name="reason" required placeholder="Rejection reason" class="rounded-lg border px-3 text-sm"><button class="rounded-lg bg-red-700 px-4 py-2 text-sm font-bold text-white">Reject</button></form></div>@endif</div></article>@endforeach</div><div class="mt-8">{{ $verifications->links() }}</div></div>@endsection