<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="csrf-token" content="{{ csrf_token() }}"><title>{{ $title ?? 'DelsuMart' }} — Secure DELSU Marketplace</title>@vite(['resources/css/app.css','resources/js/app.js'])</head>
<body class="min-h-screen bg-slate-50 text-slate-900"><header class="sticky top-0 z-40 border-b border-slate-200 bg-white/95 backdrop-blur"><div class="mx-auto flex h-16 max-w-7xl items-center gap-7 px-4 sm:px-6"><a href="{{ route('home') }}" class="text-xl font-black tracking-tight text-blue-950">delsu<span class="text-blue-700">mart</span></a><nav class="hidden items-center gap-6 text-sm font-semibold md:flex"><a href="{{ route('listings.index') }}" class="hover:text-blue-800">Marketplace</a><a href="{{ route('home') }}#security" class="hover:text-blue-800">How it works</a></nav><div class="ml-auto flex items-center gap-3">@auth<a href="{{ auth()->user()->isAdmin()?route('admin.dashboard'):route('dashboard') }}" class="text-sm font-semibold">Dashboard</a><form method="POST" action="{{ route('logout') }}">@csrf<button class="rounded-lg border px-4 py-2 text-sm font-semibold">Sign out</button></form>@else<a href="{{ route('login') }}" class="text-sm font-semibold">Sign in</a><a href="{{ route('register') }}" class="rounded-lg bg-blue-950 px-4 py-2 text-sm font-bold text-white">Join DelsuMart</a>@endauth</div></div></header>
@if(session('success'))<div class="mx-auto mt-4 max-w-7xl px-4"><div class="border-l-4 border-emerald-600 bg-emerald-50 p-4 text-sm text-emerald-900">{{ session('success') }}</div></div>@endif
@if(session('warning'))<div class="mx-auto mt-4 max-w-7xl px-4"><div class="border-l-4 border-amber-500 bg-amber-50 p-4 text-sm">{{ session('warning') }}</div></div>@endif
<main>{{ $slot ?? '' }}@yield('content')</main><footer class="mt-20 border-t bg-blue-950 text-slate-300"><div class="mx-auto grid max-w-7xl gap-8 px-4 py-10 sm:grid-cols-2"><div><div class="text-xl font-black text-white">delsumart</div><p class="mt-2 max-w-md text-sm">A security-first peer-to-peer marketplace for verified Delta State University students.</p></div><div class="sm:text-right"><p class="text-sm">Verified students. Protected transactions. Accountable trading.</p></div></div></footer></body></html>